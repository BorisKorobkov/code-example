const assert = require('assert');
const {_} = require('../src/_');

describe('_', () => {

    it('Sample Input', () => {
        let output = _();
        assert.equal(output, undefined);
    });
});